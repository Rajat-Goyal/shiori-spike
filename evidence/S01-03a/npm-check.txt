S01-03a independent QA — commitment mode repair
Captured: 2026-07-26T18:15:16Z
Implementation commit: b7252c5ba83e10f2d851b891e345059b605993bf
Runtime: Node v26.5.0; npm 11.17.0; Vitest 3.2.7; Playwright 1.61.1

Exact focused verification
1. npm run test --workspace @shiori/server -- src/decision/engine.test.ts
   PASS — 1 file, 103 tests.
2. npm run test --workspace @shiori/server -- src/telegram/conversation.test.ts
   PASS — 1 file, 42 tests.

Full repository gate
3. npm run check
   PASS — server and web typechecks; 24 server files / 382 tests;
   22 release-verifier tests; server and web production builds; 50 isolated
   Playwright tests across desktop and mobile Chromium.

The first sandboxed full-gate attempt reached Playwright after typechecks,
server tests, release-verifier tests, and builds passed, but macOS denied the
isolated local server bind with EPERM. The exact command was rerun with local
binding permission and passed completely.

Independent repair checks
- The provider input and output contract contains one commitmentMode enum with
  exactly unresolved, simple_action, and possible_work_session.
- The provider-facing schema contains neither simpleAction nor
  possibleWorkSession. Existing application/persistence booleans remain behind
  an explicit conversation-boundary compatibility mapping.
- Incomplete definition/target candidates require unresolved; complete
  candidates require simple_action or possible_work_session.
- A definition-only first turn creates an awaiting_target draft. A target-only
  continuation receives that same versioned draft as bounded provider context
  and can update it to complete.
- Complete simple-action and possible-work-session routes remain covered.
- Invalid mode/action/core-field combinations fail semantic validation.
- The repaired path writes only a draft before presenting confirmation; it
  creates no commitment, session, reminder, event, or scheduled message.
- The implementation commit adds no provider retry, migration, database schema
  change, deployment, or external-system mutation.
- No live OpenAI smoke was run for this repair, as explicitly instructed.

Acceptance criteria
- AC1 PASS — all three classes and repaired mode/continuation fixtures pass.
- AC2 PASS — the strict stateless request contract remains configured,
  tool-free, store false, and bounded to one 30-second provider request.
- AC3 PASS — repaired enum, phase, action, and core-field invariants fail closed.
- AC4 PASS — all bounded failure classes remain non-authorizing and preserve
  the current conversation snapshot.
- AC5 PASS — deterministic repair verification retained no raw provider input,
  output, trace, credential, or chain of thought and performed no product
  mutation. The previously recorded single-provider smoke was not repeated.

Deviations
- QA ran on Node v26.5.0, which is inside the package manifest's supported
  >=24 <27 range but differs from the repository guidance's preferred Node 24.
- The live provider smoke was intentionally not rerun for this repair.
